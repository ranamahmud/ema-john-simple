import React from 'react';

const ReviewItem = (props) => {
    console.log(props);
    return (
        <div>
            <h4>This is to review</h4>
        </div>
    );
};

export default ReviewItem;